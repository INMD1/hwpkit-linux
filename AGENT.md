# Linux 패키징 작업 메모

- hwpkit 라이브러리와 연구 폴더의 파일 변환 어댑터/PDF 비교기를 읽어서 독립 배포물을 만든다.
- 원본 데이터셋, 가상환경, 모델, 사용자 문서는 배포물에 포함하지 않는다.
- 94%는 PDF 비교의 목표값이며 달성한 품질로 표시하지 않는다.
- 배포물은 기존 저장소 밖에 풀어서 검증한다. 실패·경로 공백·기존 파일 보호도 확인한다.

## 2026-09-06 생성 및 검증

- CLI 기본 프로젝트. scripts/build.py가 현재 hwpkit/src를 esbuild로 의존성과 함께 묶고 DOC 어댑터/PDF 비교기를 포함.
- Linux tar.gz와 Debian all 패키지, SHA-256, 파일별 manifest, 재빌드용 소스/lockfile/라이선스 포함.
- 형제 저장소와 데이터셋은 읽기만 수행. 시스템 설치나 apt 변경 없음.
- 배포 대상에는 Python 3.10+, Node 18+가 필요. PDF 출력/DOC는 LibreOffice, 비교는 PyMuPDF 필요.
  setup.sh는 사용자 전용 venv에 비교 의존성을 설치하며 최초 설치 때만 네트워크 사용.
- 한컴 제작 원본 PDF가 기본 기준. 목표 0.94는 미달 상태이며 진단 결과를 달성 정확도로 표시하지 않음.
- 독립 압축 해제 경로에서 실제 6개 통합 검사: 한글/공백 경로·MD↔DOCX·DOC 왕복·PDF 출력/비교·출처 거부·파일 보호.
  검증 환경 Ubuntu 26.04, Node 24, Python 3.14, LibreOffice 26.2, PyMuPDF 1.28.
- CLI 코드 0=성공/목표 통과, 1=비교 완료/목표 미달, 2=실행 오류. 일부 선택 의존성 미설치 시 doctor도 2.
- GUI/AppImage 및 타 배포판 실기 설치는 구현/검증하지 않음. deb는 메타데이터·압축 해제 실행으로 검증.
